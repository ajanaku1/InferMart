# Dependencies
node_modules/

# Secrets — never commit
.env
.env.*
!.env.example

# Local runtime state (seeds, keys, logs, coordination files)
.spike/
.qvac/
*.log
logs/

# Models (large binaries, fetched at runtime)
*.gguf
models/

# Build artifacts
dist/
build/
artifacts/
cache/
# generated provider worker bundle (npm run bundle-worker)
qvac/

# Local tooling / editor
.claude/
.codewhale/
ai/
.vercel
.DS_Store
proposals/

# Video files (renders + screen recordings) — never commit
*.mp4
*.webm
*.mov
*.m4v
video/

# Landing deploy artifacts
landing/demo.mp4
landing/.vercel/
.vercel/

# build-prompt scaffolding (local only)
Goal.md
prompt.md
plan.md
submission.md
